coverage:
  status:
    project:
      default:
        target: auto
        threshold: 99
        base: auto
        flags:
          - unit
        paths:
          - src
        branches:
          - main
        if_ci_failed: error
        informational: false
        only_pulls: false
comment: true
